{
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"tasks": {
		"build": {
			"inputs": ["blog/**", "docs/**", "src/**", "static/**", "docusaurus.config.ts"],
			"outputs": ["build/**"]
		}
	}
}
